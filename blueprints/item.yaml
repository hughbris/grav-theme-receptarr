title: Item
# this is just included for safety; rename your item pages to blog-post some day
'@extends':
    type: blog-post
    context: blueprints://pages

form:
    fields:
        tabs:

            fields:

                content:
                    fields:
                        deprecation.tip:
                            type: display
                            label: false
                            content: ADMIN.DEPRECATIONS.ITEM
                            markdown: true
                            classes: tip warning
                            ordering@: header.title
